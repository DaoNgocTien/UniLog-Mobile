<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.unilog">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".LogDetailActivity"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog"
            android:parentActivityName=".ListLogActivity"></activity>
        <activity
            android:name=".ListEmployeeActivity"
            android:parentActivityName=".DashBoardActivity" />
        <activity
            android:name=".ListLogActivity"
            android:parentActivityName=".ListApplicationInstanceActivity" />
        <activity
            android:name=".ListApplicationInstanceActivity"
            android:parentActivityName=".ListApplicationActivity" />
        <activity
            android:name=".ListApplicationActivity"
            android:parentActivityName=".DashBoardActivity" />
        <activity
            android:name=".DashBoardActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>